# Use 'if' when you want to run a block of code only if a condition is true.

age = int(input("Enter your age: "))

if age >= 18:
    print("You're eligible to vote.")

# If the condition is false, Python just skips the block.